import {render,waitFor,fireEvent} from "@testing-library/react";
import {{pascalCase name}} from "./{{pascalCase name}}";

test("Unit test for <{{pascalCase name}}/> component", async() => {
    render(<{{pascalCase name}}/>)
})